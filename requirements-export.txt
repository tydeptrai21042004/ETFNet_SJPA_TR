# Optional portable export/runtime dependencies.
# Install only the formats you need. TensorRT must be installed from NVIDIA
# for the exact CUDA/TensorRT platform and is intentionally not pinned here.
onnx>=1.16.0
onnxruntime>=1.17.0
onnxslim>=0.1.34
openvino>=2024.0.0
